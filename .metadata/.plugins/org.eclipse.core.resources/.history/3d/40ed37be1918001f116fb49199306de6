<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Login</title>
    <link rel="stylesheet" type="text/css" href="/css/styles.css"/>
</head>
<body>
    <div class="container">
        <div class="signup-content">
            <div class="image"></div>
            <div class="form">
                <h2>Login</h2>
                <form th:action="@{/login}" method="post">
                    <input type="text" id="username" name="username" placeholder="Username" th:value="${username}" required/>
                    <input type="password" id="password" name="password" placeholder="Password" th:value="${password}" required/>
                    <select id="role" name="role" required>
                        <option th:each="role : ${roles}" th:value="${role.description.name()}" th:text="${role.description.name()}" th:selected="${role.description.name() == role}"></option>
                    </select>
                    <button type="submit">Login</button>
                </form>
                <div th:if="${error}" class="error">
                    <p th:text="${error}"></p>
                </div>
                <a th:href="@{/register}">Register</a>
                <a th:href="@{/forgot-password}">Forgot Password</a>
                <a th:href="@{/change-password}">Change Password</a>
            </div>
        </div>
    </div>
</body>
</html>
